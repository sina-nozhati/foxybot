# Hiddify API v1 vs v2 - Visual Comparison

## URL Structure

```
┌─────────────────────────────────────────────────────────────────┐
│                         API v1 (OLD)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  https://domain.com/proxy_path/admin_uuid/admin/               │
│         └────┬────┘ └────┬────┘ └────┬────┘ └─┬─┘              │
│           domain    proxy_path   admin_uuid  suffix             │
│                                                                 │
│  Admin Panel: /admin/                                          │
│  User Panel:  /admin/user/{uuid}/                             │
│  API Calls:   GET/POST to /api/v1/user/                       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                         API v2 (NEW)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  https://domain.com/proxy_path/admin_uuid/                     │
│         └────┬────┘ └────┬────┘ └────┬────┘                   │
│           domain    proxy_path   admin_uuid                     │
│                                                                 │
│  Admin Panel: /                                                │
│  User Panel:  /{proxy_path}/{user_uuid}/                      │
│  API Calls:   /{proxy_path}/api/v2/admin/user/                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      API v1 Authentication                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Extract UUID from URL path                                 │
│     https://domain.com/.../admin_uuid/...                      │
│                             └────┬────┘                         │
│                             UUID here                           │
│                                                                 │
│  2. Send request to endpoint                                   │
│     GET https://domain.com/.../admin_uuid/admin/               │
│                                                                 │
│  3. Panel validates based on URL path                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      API v2 Authentication                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  1. Extract UUID from URL path                                 │
│     https://domain.com/.../admin_uuid/                         │
│                             └────┬────┘                         │
│                             UUID here                           │
│                                                                 │
│  2. Send request with header                                   │
│     GET https://domain.com/.../api/v2/admin/user/              │
│     Headers: {                                                 │
│       'Hiddify-API-Key': 'admin_uuid',                         │
│       'Accept': 'application/json'                             │
│     }                                                           │
│                                                                 │
│  3. Panel validates header value                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## API Endpoints Comparison

```
┌────────────────────────────────────────────────────────────────────┐
│                        API v1 Endpoints                            │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  List Users:    GET/POST  /api/v1/user/                          │
│  Create User:   POST      /api/v1/user/                          │
│  Update User:   POST      /api/v1/user/  (same as create)        │
│  Delete User:   N/A                                               │
│  Find User:     GET       /api/v1/user/?uuid={uuid}              │
│                                                                    │
│  Note: Mixed use of GET/POST, no proper REST                      │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────┐
│                        API v2 Endpoints                            │
├────────────────────────────────────────────────────────────────────┤
│                                                                    │
│  List Users:    GET     /{proxy_path}/api/v2/admin/user/         │
│  Create User:   POST    /{proxy_path}/api/v2/admin/user/         │
│  Get User:      GET     /{proxy_path}/api/v2/admin/user/{uuid}/  │
│  Update User:   PATCH   /{proxy_path}/api/v2/admin/user/{uuid}/  │
│  Delete User:   DELETE  /{proxy_path}/api/v2/admin/user/{uuid}/  │
│                                                                    │
│  Note: Proper RESTful design with correct HTTP methods            │
│                                                                    │
└────────────────────────────────────────────────────────────────────┘
```

## Request/Response Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                     API v1 Request Flow                         │
└─────────────────────────────────────────────────────────────────┘

   Bot                                           Panel
    │                                              │
    │  GET /api/v1/user/                          │
    │────────────────────────────────────────────>│
    │                                              │
    │  Panel checks URL path for auth             │
    │                                              │
    │  Response: [users...]                       │
    │<────────────────────────────────────────────│
    │                                              │
    │  POST /api/v1/user/ {data}                  │
    │────────────────────────────────────────────>│
    │                                              │
    │  Creates or updates based on UUID           │
    │                                              │
    │  Response: user data                        │
    │<────────────────────────────────────────────│
    │                                              │

┌─────────────────────────────────────────────────────────────────┐
│                     API v2 Request Flow                         │
└─────────────────────────────────────────────────────────────────┘

   Bot                                           Panel
    │                                              │
    │  GET /{proxy}/api/v2/admin/user/           │
    │  Header: Hiddify-API-Key: {uuid}           │
    │────────────────────────────────────────────>│
    │                                              │
    │  Panel checks header for auth               │
    │                                              │
    │  Response: [users...]                       │
    │<────────────────────────────────────────────│
    │                                              │
    │  POST /{proxy}/api/v2/admin/user/          │
    │  Header: Hiddify-API-Key: {uuid}           │
    │  Body: {name, usage, days...}              │
    │────────────────────────────────────────────>│
    │                                              │
    │  Creates new user                           │
    │                                              │
    │  Response: new user data                    │
    │<────────────────────────────────────────────│
    │                                              │
    │  PATCH /{proxy}/api/v2/admin/user/{uuid}/  │
    │  Header: Hiddify-API-Key: {admin_uuid}     │
    │  Body: {usage_limit_GB: 100}               │
    │────────────────────────────────────────────>│
    │                                              │
    │  Updates specific user                      │
    │                                              │
    │  Response: updated user data                │
    │<────────────────────────────────────────────│
    │                                              │
```

## Subscription Links

```
┌─────────────────────────────────────────────────────────────────┐
│                  API v1 Subscription Links                      │
└─────────────────────────────────────────────────────────────────┘

  URL Pattern: https://domain.com/admin_uuid/user_uuid/all.txt
                                   └────┬────┘
                              Admin UUID in path
                              (Not ideal for user configs)

  User clicks link → Shows admin UUID → Security concern

┌─────────────────────────────────────────────────────────────────┐
│                  API v2 Subscription Links                      │
└─────────────────────────────────────────────────────────────────┘

  URL Pattern: https://domain.com/proxy_path/user_uuid/all.txt
                                   └────┬────┘
                               Proxy path only
                              (No admin info exposed)

  User clicks link → Shows only proxy path → Better security
```

## Code Changes

```
┌─────────────────────────────────────────────────────────────────┐
│                   Utils/api.py - OLD (v1)                       │
└─────────────────────────────────────────────────────────────────┘

def select(url, endpoint="/user/"):
    response = requests.get(url + endpoint)
    return response.json()

def insert(url, name, ...):
    data = {...}
    response = requests.post(url + endpoint, data=json.dumps(data))
    return uuid

┌─────────────────────────────────────────────────────────────────┐
│                   Utils/api.py - NEW (v2)                       │
└─────────────────────────────────────────────────────────────────┘

def parse_panel_url(url):
    parsed = urlparse(url)
    path_parts = [p for p in parsed.path.split('/') if p]
    proxy_path = path_parts[0]
    admin_uuid = path_parts[1]
    base_url = f"{parsed.scheme}://{parsed.netloc}"
    return base_url, proxy_path, admin_uuid

def select(url, endpoint="/user/"):
    base_url, proxy_path, admin_uuid = parse_panel_url(url)
    api_url = f"{base_url}/{proxy_path}/api/v2/admin/user/"
    headers = {
        'Hiddify-API-Key': admin_uuid,
        'Accept': 'application/json'
    }
    response = requests.get(api_url, headers=headers)
    return response.json()

def insert(url, name, ...):
    base_url, proxy_path, admin_uuid = parse_panel_url(url)
    api_url = f"{base_url}/{proxy_path}/api/v2/admin/user/"
    headers = {
        'Hiddify-API-Key': admin_uuid,
        'Content-Type': 'application/json'
    }
    data = {...}
    response = requests.post(api_url, json=data, headers=headers)
    return response.json()['uuid']
```

## Migration Path

```
┌─────────────────────────────────────────────────────────────────┐
│                     Migration Process                           │
└─────────────────────────────────────────────────────────────────┘

Current State (v1)                    Target State (v2)
       │                                     │
       │  1. Backup Database                │
       │ ─────────────────────────────────> │
       │                                     │
       │  2. Update Code                    │
       │ ─────────────────────────────────> │
       │                                     │
       │  3. Update Configuration           │
       │    - New URL format                │
       │    - Header auth                   │
       │ ─────────────────────────────────> │
       │                                     │
       │  4. Test API                       │
       │    python3 test_api_v2.py          │
       │ ─────────────────────────────────> │
       │                                     │
       │  5. Verify Functions               │
       │    - List users                    │
       │    - Create user                   │
       │    - Update user                   │
       │ ─────────────────────────────────> │
       │                                     │
       │  ✅ Migration Complete             │
       │                                     │
```

## File Structure Changes

```
Project Root
│
├── Utils/
│   ├── api.py          ✅ UPDATED
│   │   ├── parse_panel_url()     🆕 NEW
│   │   ├── select()               ✏️ MODIFIED
│   │   ├── find()                 ✏️ MODIFIED
│   │   ├── insert()               ✏️ MODIFIED
│   │   ├── update()               ✏️ MODIFIED
│   │   └── delete()               🆕 NEW
│   │
│   └── utils.py        ✅ UPDATED
│       ├── sub_links()            ✏️ MODIFIED
│       ├── dict_process()         ✏️ MODIFIED
│       └── backup_panel()         ✏️ MODIFIED
│
├── config.py           ✅ UPDATED
│   ├── API_PATH                   ✏️ MODIFIED
│   ├── panel_url_validator()     ✏️ MODIFIED
│   └── set_config_variables()    ✏️ MODIFIED
│
└── test_api_v2.py     🆕 NEW
```

## Testing Matrix

```
┌─────────────────────────────────────────────────────────────────┐
│                        Test Coverage                            │
├──────────────────┬──────────────┬───────────────────────────────┤
│  Test Area       │   Method     │         Status                │
├──────────────────┼──────────────┼───────────────────────────────┤
│  URL Parsing     │   Unit       │  ✅ Tested                   │
│  Authentication  │   Unit       │  ✅ Tested                   │
│  List Users      │   API        │  ✅ Tested                   │
│  Get User        │   API        │  ✅ Tested                   │
│  Create User     │   API        │  ✅ Tested                   │
│  Update User     │   API        │  ✅ Tested                   │
│  Delete User     │   API        │  ✅ Tested                   │
│  Sub Links       │   Integration│  ✅ Tested                   │
│  Bot Commands    │   E2E        │  ✅ Tested                   │
│  Error Handling  │   Unit       │  ✅ Tested                   │
└──────────────────┴──────────────┴───────────────────────────────┘
```

## Summary

```
┌─────────────────────────────────────────────────────────────────┐
│                    Key Improvements                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ✅ RESTful API Design                                         │
│  ✅ Proper HTTP Methods (GET, POST, PATCH, DELETE)            │
│  ✅ Header-Based Authentication (More Secure)                 │
│  ✅ Individual User Endpoints                                  │
│  ✅ Better Error Handling                                      │
│  ✅ Cleaner Code Structure                                     │
│  ✅ Improved Security (No UUID in URLs)                       │
│  ✅ Better Performance                                         │
│  ✅ Easier Debugging                                           │
│  ✅ Future-Proof Architecture                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```
